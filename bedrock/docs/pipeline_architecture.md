# Pipeline Architecture

Pipeline chain implemented in code:
`parcel -> zoning -> layout -> feasibility`

For authoritative runtime status and gate outcome, use:
- `bedrock/docs/system_truth_snapshot.md`
